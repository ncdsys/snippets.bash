#create #generate #take #set #git #new #existing #branch #commit

# create a new git branch from existing commit.
cd <folder>
git checkout master
git checkout -b <branch-name> <commit>
git push --set-upstream origin <branch-name>

