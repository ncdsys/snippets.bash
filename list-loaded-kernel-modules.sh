##############################################################################
# list #loaded #kernel #modules

# list currently loaded modules in the kernel.
# kernel modules
sudo bash -i
lsmod | less

